<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.craftsman.productsvc.repository.ProductSvcRepository">

	<select id="findById"
		resultType="com.craftsman.productsvc.domain.model.persistence.ProductSvcEntity">
		SELECT id, name, description FROM product WHERE id = #{id}
	</select>

	<select id="findAll"
		resultType="com.craftsman.productsvc.domain.model.persistence.ProductSvcEntity">
		SELECT id, name, description FROM product
	</select>

	<insert id="save" keyColumn="id" keyProperty="id"
		parameterType="com.craftsman.productsvc.domain.model.persistence.ProductSvcEntity"
		useGeneratedKeys="true"> INSERT INTO product <trim prefix="(" suffix=")"
			suffixOverrides=",">
			<if test="id != null and id != '' ">
				id,
			</if>
			<if test="name != null">
				name,
			</if>
			<if test="description != null and description != '' ">
				description,
			</if>
		</trim>
		<trim prefix="values ("
			suffix=")" suffixOverrides=",">
			<if test="id != null and id != '' ">
				#{id},
			</if>
			<if test="name != null">
				#{name},
			</if>
			<if test="description != null and description != '' ">
		#{description},
			</if>
		</trim>
	</insert>

	<update id="update"
		parameterType="com.craftsman.productsvc.domain.model.persistence.ProductSvcEntity">
		UPDATE product <set>
			name = #{name},
			description = #{description}
		</set> WHERE id =
		#{id} </update>

	<delete id="delete">
		DELETE FROM product
		WHERE id = #{id}
	</delete>

</mapper>